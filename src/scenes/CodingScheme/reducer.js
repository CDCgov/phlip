const INITIAL_STATE = {
  questions: []
}

const codingSchemeReducer = (state = INITIAL_STATE, action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default codingSchemeReducer